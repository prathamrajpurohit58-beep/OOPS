Q) Write a code of Fuction overloading 
Ans)

#include <iostream>
using namespace std;

class Student
{
    private: //Abstraction is used
// properties/data  members
string name;                     
int age;         // encapsulation is uesd means string, age and roll_no are all in one place
int roll_no;

public:


int area ( int l, int b)
{


    return (l*b);

}

int area (int a )
{
return (a*a);
}

};
int main ()
{

    Student obj;
    cout<<obj.area(2,3)<<endl;

    cout<<obj.area(2);
    return 0;
}

